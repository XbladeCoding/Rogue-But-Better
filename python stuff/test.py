c1,g1 = map(int,input('Enter stock and weight in g, separated by spaces.').split(' '))
c2,g2 = map(int,input('Enter stock and weight in g, separated by spaces.').split(' '))
k = int(input('Enter total storage in g.'))

# c1 * g1 + c2 * g2 <= k